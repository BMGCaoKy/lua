---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Anybook.
--- DateTime: 2022/5/12 19:46
---

local ease = require("core.enum").easeType
Enum.EaseType = setmetatable({}, {
    __index = function(_, key)
        if not key then
            return nil
        end
        if ease[key] then
            return ease[key]
        end
        local lowerKey = key:sub(0, 1):lower() .. key:sub(2)
        if ease[lowerKey] then
            return ease[lowerKey]
        end
        Lib.logError("EaseType does not exist !", key)
        return nil
    end,
    __newindex = function()
    end
})

Enum.TweenPlayStatus = {
    None = "None",
    Delayed = "Delayed",
    Playing = "Playing",
    Cancelled = "Cancelled",
    Paused = "Paused",
    Completed = "Completed",
}

Enum.TweenPlayMode = {
    Positive = "Positive",
    Yoyo = "Yoyo",
    Negative = "Negative",
}